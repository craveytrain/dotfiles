---
phase: 05-fish-conf-d-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/fish/files/.config/fish/config.fish
  - modules/fish/files/.config/fish/conf.d/10-fish-core.fish
  - modules/fish/files/.config/fish/functions/mux.fish
  - modules/fish/files/.config/fish/functions/digg.fish
  - modules/fish/files/.config/fish/functions/headers.fish
  - modules/fish/files/.config/fish/functions/l.fish
  - modules/fish/files/.config/fish/functions/la.fish
  - modules/fish/files/.config/fish/functions/lanscan.fish
  - modules/fish/files/.config/fish/functions/lk.fish
  - modules/fish/files/.config/fish/functions/ll.fish
  - modules/fish/files/.config/fish/functions/ls.fish
  - modules/fish/files/.config/fish/functions/lt.fish
  - modules/fish/files/.config/fish/functions/lu.fish
  - modules/fish/files/.config/fish/functions/multicd.fish
  - modules/fish/files/.config/fish/functions/port.fish
  - modules/editor/files/.config/fish/conf.d/50-editor-abbrs.fish
  - modules/editor/files/.config/fish/config.fish
  - modules/shell/files/.config/fish/conf.d/50-shell-eza-colors.fish
  - modules/shell/files/.config/fish/config.fish
  - modules/dev-tools/files/.config/fish/conf.d/80-dev-tools-mise.fish
  - modules/dev-tools/files/.config/fish/config.fish
autonomous: true
requirements: [SHRC-02]

must_haves:
  truths:
    - "Fish conf.d fragments exist for all 4 modules (fish, editor, shell, dev-tools)"
    - "Fish config.fish is minimal skeleton with only config.local.fish source"
    - "mux function extracted to its own functions/mux.fish file"
    - "All function files have attribution comment headers"
    - "Old monolith config.fish files in editor, shell, dev-tools modules are removed"
  artifacts:
    - path: "modules/fish/files/.config/fish/conf.d/10-fish-core.fish"
      provides: "Core env vars, abbrs, tide prompt config"
      contains: "fish module"
    - path: "modules/editor/files/.config/fish/conf.d/50-editor-abbrs.fish"
      provides: "ia and marked abbrs"
      contains: "editor module"
    - path: "modules/shell/files/.config/fish/conf.d/50-shell-eza-colors.fish"
      provides: "EZA_COLORS env var"
      contains: "shell module"
    - path: "modules/dev-tools/files/.config/fish/conf.d/80-dev-tools-mise.fish"
      provides: "mise activate for fish"
      contains: "dev-tools module"
    - path: "modules/fish/files/.config/fish/functions/mux.fish"
      provides: "Extracted mux function"
      contains: "function mux"
    - path: "modules/fish/files/.config/fish/config.fish"
      provides: "Minimal skeleton"
      contains: "config.local.fish"
  key_links:
    - from: "modules/fish/files/.config/fish/conf.d/10-fish-core.fish"
      to: "brew shellenv, fish_add_path"
      via: "PATH setup before other fragments"
      pattern: "brew shellenv"
    - from: "modules/dev-tools/files/.config/fish/conf.d/80-dev-tools-mise.fish"
      to: "mise activate"
      via: "late-loading after PATH established"
      pattern: "mise activate fish"
---

<objective>
Create fish conf.d fragment files for all 4 modules and reduce config.fish to a minimal skeleton.

Purpose: Move all fish module contributions from monolith config.fish files into native conf.d fragments so edits go live on git pull without Ansible redeploy.
Output: 4 conf.d fragment files, extracted mux.fish function, minimal config.fish skeleton, attribution headers on all function files.
</objective>

<execution_context>
@/Users/mcravey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcravey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fish-conf-d-migration/05-CONTEXT.md
@.planning/phases/05-fish-conf-d-migration/05-RESEARCH.md
@.planning/phases/04-zsh-conf-d-migration/04-01-SUMMARY.md

Key source files to read before implementing:
@modules/fish/files/.config/fish/config.fish
@modules/editor/files/.config/fish/config.fish
@modules/shell/files/.config/fish/config.fish
@modules/dev-tools/files/.config/fish/config.fish
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conf.d fragment files for all 4 modules and extract mux function</name>
  <files>
    modules/fish/files/.config/fish/conf.d/10-fish-core.fish
    modules/editor/files/.config/fish/conf.d/50-editor-abbrs.fish
    modules/shell/files/.config/fish/conf.d/50-shell-eza-colors.fish
    modules/dev-tools/files/.config/fish/conf.d/80-dev-tools-mise.fish
    modules/fish/files/.config/fish/functions/mux.fish
  </files>
  <action>
Create 4 conf.d fragment files and extract the mux function. Follow the Phase 4 pattern: attribution header comment, then content. No shellcheck directive needed (fish, not bash).

**10-fish-core.fish** (fish module): Contains all content from current config.fish monolith EXCEPT the mux function and config.local.fish source. Split content by guard need:
- Unguarded section: env vars (DOTFILES, XDG_CONFIG_HOME), brew shellenv, fish_add_path calls, CDPATH, LS_COLORS. These should be available in non-interactive fish scripts too (current monolith incorrectly wraps them in interactive guard).
- `if status --is-interactive` guarded section: abbreviations (dot, dotdot, ip, pubkey, mkdir, df, du) and tide prompt config (tide_left_prompt_items, tide_right_prompt_items).
- Attribution header: `# fish module - core environment, abbreviations, and prompt configuration`

**50-editor-abbrs.fish** (editor module): ia and marked abbreviations wrapped in `if status --is-interactive` guard.
- Attribution header: `# editor module - program launcher abbreviations`

**50-shell-eza-colors.fish** (shell module): EZA_COLORS env var, no interactive guard needed.
- Attribution header: `# shell module - eza color configuration`

**80-dev-tools-mise.fish** (dev-tools module): Use `mise activate fish | source` (NOT `--shims`) wrapped in `if status --is-interactive` guard. This follows mise's recommendation for interactive shells and provides full feature set (hooks, watch_files, env var loading on cd). The current `status --is-login` with `--shims` is the old approach.
- Attribution header: `# dev-tools module - mise runtime manager activation`

**functions/mux.fish**: Extract the inline mux function from config.fish into its own file following fish autoload convention (one function per file). Add attribution header: `# fish module - create or attach to a named tmux session`

Use the exact code examples from 05-RESEARCH.md as reference for content and formatting.

Create the conf.d directories as needed (they don't exist yet in the module file trees).
  </action>
  <verify>
    <automated>cd /Users/mcravey/conductor/workspaces/dotfiles/tunis && test -f modules/fish/files/.config/fish/conf.d/10-fish-core.fish && test -f modules/editor/files/.config/fish/conf.d/50-editor-abbrs.fish && test -f modules/shell/files/.config/fish/conf.d/50-shell-eza-colors.fish && test -f modules/dev-tools/files/.config/fish/conf.d/80-dev-tools-mise.fish && test -f modules/fish/files/.config/fish/functions/mux.fish && grep -q "fish module" modules/fish/files/.config/fish/conf.d/10-fish-core.fish && grep -q "editor module" modules/editor/files/.config/fish/conf.d/50-editor-abbrs.fish && grep -q "shell module" modules/shell/files/.config/fish/conf.d/50-shell-eza-colors.fish && grep -q "dev-tools module" modules/dev-tools/files/.config/fish/conf.d/80-dev-tools-mise.fish && grep -q "function mux" modules/fish/files/.config/fish/functions/mux.fish && grep -q "mise activate fish" modules/dev-tools/files/.config/fish/conf.d/80-dev-tools-mise.fish && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>4 conf.d fragments created with attribution headers, mux function extracted to functions/mux.fish. Env vars unguarded, abbrs/prompt/mise guarded with `if status --is-interactive`.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite config.fish to skeleton, remove old module config.fish files, add attribution headers to function files</name>
  <files>
    modules/fish/files/.config/fish/config.fish
    modules/editor/files/.config/fish/config.fish
    modules/shell/files/.config/fish/config.fish
    modules/dev-tools/files/.config/fish/config.fish
    modules/fish/files/.config/fish/functions/digg.fish
    modules/fish/files/.config/fish/functions/headers.fish
    modules/fish/files/.config/fish/functions/l.fish
    modules/fish/files/.config/fish/functions/la.fish
    modules/fish/files/.config/fish/functions/lanscan.fish
    modules/fish/files/.config/fish/functions/lk.fish
    modules/fish/files/.config/fish/functions/ll.fish
    modules/fish/files/.config/fish/functions/ls.fish
    modules/fish/files/.config/fish/functions/lt.fish
    modules/fish/files/.config/fish/functions/lu.fish
    modules/fish/files/.config/fish/functions/multicd.fish
    modules/fish/files/.config/fish/functions/port.fish
  </files>
  <action>
**Rewrite config.fish** (fish module): Replace the 64-line monolith with a minimal skeleton containing ONLY the config.local.fish source block:
```fish
# Local configuration (machine-specific overrides)
if test -f ~/.config/fish/config.local.fish
    source ~/.config/fish/config.local.fish
end
```
Nothing else. All module content is now in conf.d fragments. Fish sources conf.d BEFORE config.fish, so config.local.fish in config.fish runs last (correct override behavior).

**Remove old module config.fish files**: Delete these files entirely since their content has moved to conf.d fragments:
- `modules/editor/files/.config/fish/config.fish` (ia/marked abbrs now in 50-editor-abbrs.fish)
- `modules/shell/files/.config/fish/config.fish` (EZA_COLORS now in 50-shell-eza-colors.fish)
- `modules/dev-tools/files/.config/fish/config.fish` (mise activate now in 80-dev-tools-mise.fish)

**Add attribution headers to ALL existing function files** (12 files in modules/fish/files/.config/fish/functions/). Read each file first, then prepend an attribution header comment. Use format: `# fish module - brief description of what the function does`. The description should be derived from the function's behavior (read the code). Do NOT change any function logic, only add the header line before the existing content.

This completes the migration: all module content is in conf.d fragments, config.fish is minimal, and all function files have consistent attribution headers.
  </action>
  <verify>
    <automated>cd /Users/mcravey/conductor/workspaces/dotfiles/tunis && lines=$(wc -l < modules/fish/files/.config/fish/config.fish) && test "$lines" -le 5 && ! test -f modules/editor/files/.config/fish/config.fish && ! test -f modules/shell/files/.config/fish/config.fish && ! test -f modules/dev-tools/files/.config/fish/config.fish && grep -q "config.local.fish" modules/fish/files/.config/fish/config.fish && grep -q "fish module" modules/fish/files/.config/fish/functions/digg.fish && grep -q "fish module" modules/fish/files/.config/fish/functions/mux.fish && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>config.fish is minimal skeleton (config.local.fish source only). Old editor/shell/dev-tools config.fish files removed. All 13 function files (12 existing + 1 new mux.fish) have attribution comment headers.</done>
</task>

</tasks>

<verification>
- All 4 conf.d fragments exist with correct numeric prefixes (10, 50, 50, 80)
- All 4 conf.d fragments have attribution headers identifying owning module
- config.fish is <= 5 lines (config.local.fish source only)
- Old module config.fish files removed from editor, shell, dev-tools
- mux function extracted to functions/mux.fish
- All function files have attribution headers
- No content duplication between config.fish and conf.d fragments
- Env vars are unguarded; abbrs, prompt, mise are interactive-guarded
</verification>

<success_criteria>
- 4 conf.d fragment files created following NN-module-desc.fish naming convention
- Fish module config.fish reduced to minimal config.local.fish source skeleton
- mux function extracted to its own autoloaded function file
- All function files have attribution comment headers
- Old monolith config.fish files removed from editor, shell, dev-tools modules
- Content split correctly: env vars unguarded, interactive features guarded
</success_criteria>

<output>
After completion, create `.planning/phases/05-fish-conf-d-migration/05-01-SUMMARY.md`
</output>
