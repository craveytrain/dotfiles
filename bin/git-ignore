#!/bin/sh
git_root=$(git rev-parse --show-toplevel)
exclude_file="$git_root/.git/info/exclude"
pwd="$(pwd)"
git_relative_path="${pwd#$(echo $git_root)/}"

for pattern in "$@"
do
	echo "$git_relative_path/$pattern" >> "$exclude_file"
done