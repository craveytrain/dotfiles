# Mac-only git settings. Included from ~/.gitconfig via [include].
# On non-Mac machines this file isn't symlinked; git silently ignores
# the missing include.

[credential]
  helper = osxkeychain

[safe]
  directory = /opt/homebrew
