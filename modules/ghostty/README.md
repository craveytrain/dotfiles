# Ghostty

Installs the Ghostty cask and stows its config at
`~/.config/ghostty/config`.

The shared configuration defines the font, theme, window behavior, key binding,
and notification behavior. Reload Ghostty after editing the tracked file. See
the [Ghostty configuration reference](https://ghostty.org/docs/config) for
available options.
