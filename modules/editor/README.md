# Editor

Installs Vim and stows `~/.vimrc`, plus Fish and Zsh editor aliases.

Use `~/.vimrc.local` for machine-specific plugins, mappings, or settings:

```vim
" ~/.vimrc.local
set colorcolumn=100
```

The shared config loads that file when it exists.
