# .zshrc

# source every .sh & .zsh file in this repo
for config_file ($DOTFILES/**/*.zsh) do 
	if [[ $config_file == */osx/* ]]; then
		if [[ $OSTYPE == darwin* ]]; then
			source $config_file
		fi
	else
		source $config_file
	fi
done

# initialize autocomplete here, otherwise functions won't be loaded
autoload -U compinit
compinit